import { Router } from "express"
import { appendFile } from "fs"

const router = Router()

router.get('/apis/aimodels', (req, res, next) => {


})

router.post('/apis/detect/face', (req, res, next) => {


})

router.post('/apis/detect/object', (req, res, next) => {


})

export default router